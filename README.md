# mal-glass-theme
MAL Glass Theme is a modern, dark, glassmorphism-inspired custom CSS theme for MyAnimeList lists.  It focuses on a clean, minimal UI with frosted-glass backgrounds, subtle blur effects, gradient-based status indicators, and improved readability — while respecting MyAnimeList’s CSS-only customization limitations.
